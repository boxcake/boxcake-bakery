---
# User configuration overrides
# This file is automatically generated by the web configuration interface
# Manual changes will be overwritten when using the web interface

# Default values - these will be overridden by the web config generator
portainer_admin_password: "{{ portainer_admin_password | default('homelab123!') }}"
portainer_storage_size: "{{ portainer_storage_size | default('2Gi') }}"
registry_storage_size: "{{ registry_storage_size | default('10Gi') }}"

# Service toggle flags
skip_portainer: "{{ skip_portainer | default(false) }}"
skip_registry: "{{ skip_registry | default(false) }}"
skip_registry_ui: "{{ skip_registry_ui | default(false) }}"
skip_kubelish: "{{ skip_kubelish | default(false) }}"