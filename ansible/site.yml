---
# Main Ansible playbook for Home Lab Pi setup
- name: "Home Lab Pi Complete Setup"
  hosts: homelab
  become: yes
  gather_facts: yes
  
  vars_files:
    - vars/main.yml
    - vars/network.yml
  
  pre_tasks:
    - name: Update package cache
      apt:
        update_cache: yes
        cache_valid_time: 3600
      tags: always

  roles:
    - role: system-prep
      tags: [system, prep]
      
    - role: homelab-user
      tags: [user, security]
      
    - role: docker
      tags: [docker, container]
      
    - role: k3s
      tags: [k3s, kubernetes]
      
    - role: storage
      tags: [storage, longhorn]
      
    - role: load-balancer
      tags: [metallb, networking]

    - role: kubelish
      tags: [kubelish, mdns]


  post_tasks:
    - name: Display completion message
      debug:
        msg: |
          🎉 Home Lab Pi setup completed successfully!
          
          ✅ COMPLETE DEPLOYMENT - Core infrastructure running:
          - K3s Kubernetes cluster
          - Longhorn distributed storage
          - MetalLB load balancer with IP pools
          - Kubelish mDNS announcer (all nodes)
          - OpenTofu (Infrastructure as Code)

          🚧 Next Steps - Deploy with OpenTofu:
          - Docker Registry (container with persistent storage)
          - Portainer (container management)
          - Registry UI (web interface)
          - All services with proper state management
          
          🚀 Ready for OpenTofu deployment:
          1. Navigate to terraform/ directory
          2. Run 'tofu init' and 'tofu apply'
          3. Manage everything: sudo -u {{ homelab_user }} -i
          
          📊 LoadBalancer Status:
          Run: kubectl get svc -A | grep LoadBalancer
          
          🎊 Migration Complete: Pure Ansible deployment!
      tags: always
